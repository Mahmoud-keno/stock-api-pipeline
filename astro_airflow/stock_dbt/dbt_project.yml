name: 'stock_dbt'
version: '1.0.0'
profile: 'stock_dbt'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  stock_dbt:
    # Example models (if you have any)
    example:
      +materialized: view
    
    # Staging models
    staging:
      +materialized: view
      
    # Marts models  
    marts:
      +materialized: table
      